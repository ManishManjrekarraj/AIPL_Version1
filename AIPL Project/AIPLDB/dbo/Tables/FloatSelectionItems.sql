CREATE TABLE [dbo].[FloatSelectionItems] (
    [Id]   INT            IDENTITY (1, 1) NOT NULL,
    [Name] NVARCHAR (MAX) NOT NULL,
    CONSTRAINT [PK_FloatSelectionItems] PRIMARY KEY CLUSTERED ([Id] ASC)
);

